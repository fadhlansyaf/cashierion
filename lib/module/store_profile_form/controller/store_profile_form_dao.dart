import 'package:sqflite/sqflite.dart';

import '../../../model/database/category.dart';
import '../../../service/database_provider.dart';

class StoreProfileFormDao{
  // Future<void> insertCategory(CategoryModel category) async {
  //   Database db = await DatabaseProvider().database;
  //   await db.insert(DatabaseProvider.productCategoryTable, category.toJson(), conflictAlgorithm: ConflictAlgorithm.replace);
  // }

  // Future<void> editCategory(CategoryModel category) async {
  //   Database db = await DatabaseProvider().database;
  //   await db.update(DatabaseProvider.productCategoryTable, category.toJson(), conflictAlgorithm: ConflictAlgorithm.replace, where: 'product_category_id = ?', whereArgs: [category.id]);
  // }
}